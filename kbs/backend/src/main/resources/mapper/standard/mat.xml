<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kimbap.kbs.standard.mapper.MatMapper">

    <!-- 자재 목록 조회 -->
    <select id="getMatList" resultType="com.kimbap.kbs.standard.service.MatVO">
        SELECT  mcode,
                mate_name,
                mate_type,
                sto_con,
                edate
        FROM    MATERIAL
        ORDER BY mcode
    </select>

    <!-- 자재 등록 -->
    <insert id="insertMat" parameterType="com.kimbap.kbs.standard.service.MatVO">
        INSERT INTO MATERIAL (
            mcode,
            mate_ver_cd,
            mate_name,
            mate_type,
            sto_con,
            unit,
            std,
            piece_unit,
            conver_qty,
            moqty,
            safe_stock,
            edate,
            corigin,
            is_used,
            regi,
            modi,
            cha_rea,
            reg_dt,
            note
        ) VALUES (
            #{mcode, jdbcType=VARCHAR},
            #{mateVerCd, jdbcType=VARCHAR},
            #{mateName, jdbcType=VARCHAR},
            #{mateType, jdbcType=VARCHAR},
            #{stoCon, jdbcType=VARCHAR},
            #{unit, jdbcType=VARCHAR},
            #{std, jdbcType=VARCHAR},
            #{pieceUnit, jdbcType=VARCHAR},
            #{converQty, jdbcType=NUMERIC},
            #{moqty, jdbcType=NUMERIC},
            #{safeStock, jdbcType=NUMERIC},
            #{edate, jdbcType=NUMERIC},
            #{corigin, jdbcType=VARCHAR},
            #{isUsed, jdbcType=VARCHAR},
            #{regi, jdbcType=VARCHAR},
            #{modi, jdbcType=VARCHAR},
            #{chaRea, jdbcType=VARCHAR},
            SYSDATE,
            #{note, jdbcType=VARCHAR}
        )
    </insert>

    <!-- 공급사 등록 -->
    <insert id="insertMatSupplier" parameterType="com.kimbap.kbs.standard.service.MatSupplierVO">
        INSERT INTO MATE_SUPPLIER (
            mate_cp_cd,
            mcode,
            mate_ver_cd,
            cp_cd,
            unit_price,
            ltime
        ) VALUES (
            #{mateCpCd, jdbcType=VARCHAR},
            #{mcode, jdbcType=VARCHAR},
            #{mateVerCd, jdbcType=VARCHAR},
            #{cpCd, jdbcType=VARCHAR},
            #{unitPrice, jdbcType=NUMERIC},
            #{ltime, jdbcType=NUMERIC}
        )
    </insert>

    <!-- 원자재 시퀀스 -->
    <select id="getNextRawMaterialCodeBySeq" resultType="int">
        SELECT seq_raw_material_code.NEXTVAL FROM dual
    </select>

    <!-- 부자재 시퀀스 -->
    <select id="getNextSubMaterialCodeBySeq" resultType="int">
        SELECT seq_sub_material_code.NEXTVAL FROM dual
    </select>

    <select id="existsMcode" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN 1 ELSE 0 END
        FROM MATERIAL
        WHERE mcode = #{mcode}
    </select>

    <select id="getMatDetail" parameterType="string" resultType="com.kimbap.kbs.standard.service.MatVO">
        SELECT  mcode,
                mate_ver_cd,
                mate_name,
                mate_type,
                sto_con,
                unit,
                std,
                piece_unit,
                conver_qty,
                moqty,
                safe_stock,
                edate,
                corigin,
                is_used,
                regi,
                modi,
                cha_rea,
                reg_dt,
                note
        FROM    material
        WHERE   mcode = #{mcode}
    </select>
        
    <select id="getMatSuppliers" parameterType="string" resultType="com.kimbap.kbs.standard.service.MatSupplierVO">
        SELECT  mate_cp_cd,
                mcode,
                mate_ver_cd,
                cp_cd,
                unit_price,
                ltime
        FROM    MATE_SUPPLIER
        WHERE   mcode = #{mcode}
    </select>

</mapper>
